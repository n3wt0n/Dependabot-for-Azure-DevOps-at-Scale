namespace DependabotOrchestrator.Model
{
    public enum PackageManagerType
    {
        bundler, 
        cargo, 
        composer, 
        dep, 
        docker, 
        elm, 
        go_modules, 
        gradle, 
        hex, 
        maven, 
        npm_and_yarn, 
        nuget, 
        pip,
        submodules, 
        terraform
    }
}
